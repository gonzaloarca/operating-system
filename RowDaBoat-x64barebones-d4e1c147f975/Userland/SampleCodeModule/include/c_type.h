#ifndef C_TYPE_H_
#define C_TYPE_H_

// Función que devuelve distinto de 0 si c es una letra en la tabla ASCII. Devuelve 0 en caso contrario
int isalpha(int c);

// Funcion que devuelve distinto de 0 si c es un digito en la tabla ASCII. Devuelve 0 en caso contrario
int isdigit(int c);

#endif
